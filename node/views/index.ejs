<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>userinfo</title>
    </head>
    <body>
        <h1>mominfo</h1>
        <ul>
            <% for(var i=0; i<info.length; i++) { %>
                <li><%= info[i].date %> <%= info[i].user %> <%= info[i].mom %></li>
            <% } %>
        </ul>
    </body>
</html>